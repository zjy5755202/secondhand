<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.dao.UserDao">

    <insert id="addUser" parameterType="User">
        INSERT INTO users(uerId,avatar,nicekname,name,gender,grade,school,major)
        VALUES (#{uerId},#{avatar}, #{nicekname}, #{name}, #{gender},#{grade},#{school},#{major})
    </insert>

    <update id="updateUser" parameterType="User">
        UPDATE user
        SET avatar = #{avatar},nicekname = #{nicekname},grade = #{grade},school=#{school},major=#{major}
        WHERE  userId = #{userId}
    </update>

    <select id="queryById" resultType="User" parameterType="java.lang.String">
        SELECT *
        FROM user
        WHERE userid=#{userid}
    </select>
<!--    <resultMap type="user" id="userResultMap" >-->
<!--        <id property="userId" column="Id"/>-->
<!--        <result property="userName" column="name"/>-->
<!--        <result property="userPassword" column="number"/>-->
<!--        <result property="paperDetail" column="detail"/>-->
<!--    </resultMap>-->
<!--    <select id="queryByUsername" resultType="com.entity.User" parameterType="String">-->
<!--        SELECT *-->
<!--        FROM user-->
<!--        WHERE name=#{name}-->
<!--    </select>-->

</mapper>